name: Cleanup Demo Branches

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  delete-branches:
    runs-on: ubuntu-latest
    steps:
      - name: Call delete_demo_branches Edge Function
        run: |
          curl -X POST https://koaxmrtrzhilnzjbiybr.supabase.co/functions/v1/delete_demo_branches \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'